Optional autonomous AI agent integrations — external coding agent CLIs
that can be delegated to for independent coding tasks.

## Available Skills

- **edge-dynamic-persona** — Google AI Edge Gallery dynamic persona system (discussion #913). Implements `run_js` actions: `persona_bootstrap`, `trait_read/write/delete`, `data_write/read/delete`, `hook_list/register`, `execute_action`. Includes MCP server, CLI, and persistent JSON storage.
